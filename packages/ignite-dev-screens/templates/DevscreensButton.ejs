// @flow

import React from 'react'
import { TouchableOpacity, Text, View } from 'react-native'
import styles from './Styles/DevscreensButtonStyle'
import { Actions as NavigationActions } from 'react-native-router-flux'

type FullButtonProps = {
  styles?: Object
}

export default class DevscreensButton extends React.Component {
  render () {
    if (DevSettings.showDevScreens) {
      return (
        <TouchableOpacity onPress={NavigationActions.PresentationScreen}>
          <Text>Open DevScreens</Text>
        </TouchableOpacity>
      )
    } else {
      return (<View />)
    }
  }
}
