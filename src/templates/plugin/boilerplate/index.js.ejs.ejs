/**
 * This file gets copied (by default) over the freshly generated app's
 * index.ios.js and index.android.js files.
 * 
 * In this same folder, place all of the files that will be placed into your new
 * React Native project folder.
 * 
 * This is an ejs file. That means that you can type in <%= '<' + '%= props.something %' + '>' %>
 * and that property will show up in your generated projects.
 * 
 * Good luck, and if you need help, just hit up the friendly people over in
 * the Infinite Red Community Slack, http://community.infinite.red!
 * 
 */

import { AppRegistry } from 'react-native'
import App from './App/App.js'

AppRegistry.registerComponent('<%- '<' + '%= props.name %' + '>' %>', () => App)

