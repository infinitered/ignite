{
  "name": "RNBase",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "clean": "rm -rf $TMPDIR/react-* && watchman watch-del-all && npm cache clean",
    "newclear": "watchman watch-del-all && rm -rf node_modules/ && npm cache clean && npm prune && npm i"
  },
  "dependencies": {
    "react-native": "^0.20.0",
    "react-redux": "^4.4.0"
  }
}
