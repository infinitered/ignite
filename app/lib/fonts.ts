import {
  SpaceGrotesk_400Regular,
  SpaceGrotesk_500Medium,
  SpaceGrotesk_700Bold,
} from '@expo-google-fonts/space-grotesk';

/**
 * Fonts loaded by `useFonts` in `app/app.tsx`. The keys here become the
 * font-family names referenced from Tailwind in `tailwind.config.js`.
 *
 * Add weights as your design system needs them.
 */
export const customFontsToLoad = {
  SpaceGrotesk_400Regular,
  SpaceGrotesk_500Medium,
  SpaceGrotesk_700Bold,
};
