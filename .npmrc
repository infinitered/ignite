# pnpm settings tuned for Expo + React Native compatibility.
# Expo's Metro resolver doesn't fully support pnpm's strict symlink layout,
# so node-linker=hoisted is required.
auto-install-peers=true
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
package-manager-strict=true
